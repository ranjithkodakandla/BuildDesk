# Next Step

> Auto-updated after each milestone. Always read this before starting a new session.

---

## Current State

| Field              | Value                                  |
|--------------------|----------------------------------------|
| Last completed phase | Phase 5 — Frontend Realignment       |
| Git branch         | `feat/phase-5-frontend-realignment` (to be committed) |
| Test baseline      | **54 / 54 backend passing | 13/13 frontend passing**                    |
| Migration state    | `c3d4e5f6g7h8` applied (no new migrations in Phase 5) |

---

## Immediate Next Milestone

**Phase 6 — Cloud Storage & asyncpg**

Goal: Finalize backend production readiness by migrating PDF storage to Google Cloud Storage (GCS) and upgrading the database driver to `asyncpg`.

Priority tasks:
1. **GCS Integration** — Implement `CloudStorageService` to upload generated PDFs to a Google Cloud Storage bucket instead of returning bytes directly.
2. **Package Links** — Update `ProjectPackage` model to store the `storage_reference` (signed URL or gs:// link) generated by the upload.
3. **Database Driver** — Migrate SQLAlchemy configuration from synchronous `psycopg2` (or SQLite locally) to `asyncpg` for production.
4. **Endpoint Async** — Update FastAPI endpoints to use `async def` and `AsyncSession`.

Domain test: Does the system cleanly support asynchronous package generation and deliver a secure URL to the resulting fabrication package?

---

## Pending Blockers

- None.

---

## Recommended Next Prompt

```
AUTONOMOUS IMPLEMENTATION MODE — Phase 6

Mandatory startup:
1. docs/session-start.md
2. docs/domain-guardrail.md
3. docs/current-state.md

PHASE 6 GOAL: Cloud Storage & Async Backend.

Upgrade the backend to production readiness:
1. Implement GCS upload for package PDFs.
2. Update the Package generation pipeline to store `storage_reference`.
3. Migrate the database configuration to use `asyncpg`.
4. Update FastAPI endpoints to `async`.

Domain test: Packages must be reliably uploaded to the cloud and accessible via link.
If NO → reject and document in ADR rejection table.

Branch: feat/phase-6-cloud-async
```
